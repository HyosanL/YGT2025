# YGT 2025 — 게임 개발 명세서 (v2)

> 이 문서는 Claude Code에 전달하는 개발 명세서다. 이 명세를 기준으로 프로젝트를 처음부터 구축한다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 게임명 | **YGT 2025** |
| 장르 | 미니게임 모음 (탐지 회피 + 반응/운빨 게임) |
| 플랫폼 | 모바일 웹 (PWA), 세로 모드 고정 |
| 기술 스택 | Phaser 3 + TypeScript + Vite + vite-plugin-pwa |
| 호스팅/배포 | **Cloudflare Pages** (GitHub 연동 자동 배포) |
| 도메인 | **https://ygt2025.rokafa.app** (보유 도메인 `rokafa.app`의 서브도메인, Cloudflare Pages 커스텀 도메인으로 연결) |
| 리더보드 | **Cloudflare Pages Functions + D1** (같은 프로젝트 안에서 해결, 외부 서비스 없음) |
| 입력 | 터치/클릭, 텍스트 타이핑 (모바일 가상 키보드) |
| 출력 | 화면(2D), 사운드(Web Audio) |
| 아트 스타일 | 2D 미연시(비주얼 노벨) 풍 — 배경 일러스트 + 캐릭터 스탠딩 + 대사창 |

### 컨셉
군사훈련을 마친 공군사관학교 2학년 생도. 머리가 좀 커져서 이것저것 몰래 시도해본다.
**선배에게 걸리면 게임 오버.** 「선생님 몰래 춤추기」류의 "몰래 하다가 들키기 직전에 멈추기" 메커닉이 핵심.

### 코어 루프
1. **1일차부터 시작**, 하루에 메인 퀘스트 1개를 수행
2. 메인 퀘스트 성공 → 다음 날로 진행 (일수 +1)
3. 메인 퀘스트 도중 **미니 퀘스트가 무작위 발생** — 일수가 늘수록 발생 확률 증가
4. 실패(선배에게 발각 / HP 0 / 미니 퀘스트 실패) → **게임 오버**, 생존 일수를 온라인 리더보드에 등록
5. 점수 = **생존 일수** (동률 시 총 플레이 시간이 짧은 쪽이 상위)

### HP 규칙
- HP는 **하루(일차) 단위 자원**: 매일 아침 100으로 리셋
- 하루 안에서 HP 0 도달 = 게임 오버
- 회복 수단 없음 (그날 안에서는)

---

## 2. 개발 환경 제약 (중요)

- 개발 PC는 **Windows**. USB 연결 및 Mac 사용 불가 → 네이티브 앱 빌드 없음, **순수 웹 PWA**로만 개발
- 모바일 테스트 방법:
  - 로컬: `vite --host` 로 같은 Wi-Fi의 폰 브라우저에서 접속
  - 원격: Cloudflare Pages 배포본(https://ygt2025.rokafa.app)을 폰에서 접속 후 "홈 화면에 추가"
  - Pages Functions 포함 로컬 테스트: `wrangler pages dev` 사용
- Cloudflare Pages는 커스텀 도메인에 자동 HTTPS 제공 → Service Worker 정상 동작
- 커스텀 도메인 루트 서빙이므로 Vite `base: '/'` (GitHub Pages식 서브경로 설정 불필요)

---

## 3. 기술 아키텍처

### 3.1 프로젝트 구조

```
ygt2025/
├─ index.html
├─ vite.config.ts          # vite-plugin-pwa 설정, base: '/'
├─ tsconfig.json
├─ wrangler.toml           # D1 바인딩 설정
├─ public/
│  ├─ manifest.webmanifest # PWA 매니페스트 ("orientation": "portrait")
│  └─ icons/               # 192, 512 아이콘
├─ functions/              # Cloudflare Pages Functions (서버리스 API)
│  └─ api/
│     └─ leaderboard.ts    # GET(조회) / POST(등록)
├─ migrations/
│  └─ 0001_leaderboard.sql # D1 스키마
├─ src/
│  ├─ main.ts              # Phaser Game 인스턴스 생성
│  ├─ config.ts            # 게임 상수 (확률, HP, 시간 등 밸런스 값 전부 여기에)
│  ├─ types.ts
│  ├─ core/
│  │  ├─ GameState.ts      # 일수, HP(일수별 리셋), 진행 상태 (localStorage 저장)
│  │  ├─ QuestManager.ts   # 메인 퀘스트 선택/순환, 미니퀘스트 발생 판정
│  │  ├─ AudioManager.ts   # BGM/SFX, 음소거 토글
│  │  └─ Leaderboard.ts    # /api/leaderboard fetch 래퍼
│  ├─ scenes/
│  │  ├─ BootScene.ts      # 에셋 로딩 + 로딩바
│  │  ├─ TitleScene.ts     # 타이틀, 리더보드 보기, 시작
│  │  ├─ DayIntroScene.ts  # "N일차" 연출 + HP 리셋 + 오늘의 퀘스트 소개 (미연시 대사창)
│  │  ├─ ResultScene.ts    # 성공/실패 연출, 게임오버 시 리더보드 등록
│  │  ├─ main/
│  │  │  ├─ ShowerScene.ts       # 퀘스트 1: 샤워장 노래
│  │  │  ├─ HallwayScene.ts      # 퀘스트 2: 복도 인사
│  │  │  ├─ MicrowaveScene.ts    # 퀘스트 3: 전자레인지
│  │  │  ├─ WalkScene.ts         # 퀘스트 4: 태권도 걸어가기
│  │  │  └─ WallPunchScene.ts    # 퀘스트 5: 옆방 벽치기 (운빨)
│  │  └─ mini/
│  │     ├─ KakaoScene.ts        # 카톡 답장 (타자)
│  │     ├─ VoteScene.ts         # 투표 (함정 선지)
│  │     └─ PhotoPickScene.ts    # 단체 채팅방 사진 고르기
│  ├─ ui/
│  │  ├─ DialogueBox.ts    # 미연시풍 대사창 (이름표 + 텍스트 타이핑 효과)
│  │  ├─ HpBar.ts
│  │  └─ Button.ts
│  ├─ assets/              # 사용자 제공 에셋 (아래 §6 참조)
│  └─ utils/
│     ├─ rng.ts
│     └─ mobileInput.ts    # 숨겨진 <input>으로 가상 키보드 띄우기 (타자 미니게임용)
└─ (배포는 Cloudflare Pages의 GitHub 연동 CI가 담당 — 별도 워크플로 파일 불필요)
```

### 3.2 Phaser 설정

```ts
{
  type: Phaser.AUTO,
  width: 720, height: 1280,          // 9:16 세로 기준 해상도
  scale: { mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH },
  backgroundColor: '#1a1a2e',
  physics: { default: 'arcade' },     // 필요한 씬만 사용
}
```

- 모든 좌표/크기는 720×1280 기준 (FIT 스케일)
- 첫 터치 이후에만 오디오 재생 (모바일 autoplay 정책 대응)

### 3.3 상태 관리

`GameState` (싱글턴, localStorage 동기화):

```ts
interface GameState {
  day: number;              // 현재 일차 (1부터)
  hp: number;               // 0~100, 매일 100으로 리셋. 0이면 게임오버
  currentQuestId: MainQuestId | null;
  bestDay: number;          // 로컬 최고 기록
  totalPlayMs: number;      // 동률 판정용
  settings: { mute: boolean; nickname: string };
}
```

- **일차별 메인 퀘스트 배정**: 1~9일차는 퀘스트 1~4 중 랜덤(전날과 중복 금지), **10일차부터 퀘스트 5(벽치기) 포함** 랜덤
- **난이도 스케일링**: 일수가 늘수록 선배 등장 빈도↑, 반응 허용 시간↓ (config.ts에 일차별 계수 함수로 정의: `difficulty(day) = min(1, 0.3 + day * 0.07)` 같은 형태, 튜닝 대상)

### 3.4 미니 퀘스트 발생 규칙

- 메인 퀘스트 진행 중 랜덤 타이밍에 인터럽트 (메인 씬 pause, 미니 씬 오버레이 launch)
- 발생 확률: `min(0.8, 0.15 + day * 0.05)` — 하루 최대 2회
- 미니 퀘스트 실패 = 즉시 게임 오버 (config에서 HP 페널티 방식으로 전환 가능하게 상수화)
- 미니 퀘스트 종료 후 메인 씬 resume

---

## 4. 메인 퀘스트 상세 명세

공통: 각 퀘스트는 시작 전 `DayIntroScene`에서 미연시 대사창으로 상황 설명 → 터치로 시작.
공통 실패 조건: **선배에게 "몰래 행동 중" 상태로 발각되면 즉시 실패.**
공통 설계 원칙: 퀘스트는 **짧고 빠르게** 끝나야 한다 (총 플레이 10~20초 내외 목표). 선배는 **예고 없이 갑자기** 등장한다 — 사전 경고 게이지/아이콘 없음. 대신 등장 순간부터 아주 짧은 **반응 유예 시간**(0.3~0.7초, 일차↑ 시 단축) 안에 올바르게 대응해야 살아남는 **반응속도 테스트**가 핵심. 선배는 한 곳이 아니라 씬마다 **여러 등장 지점**(양쪽 문, 커튼, 복도 곳곳 등) 중 무작위로 나타난다.

### Q1. 샤워장에서 몰래 노래 틀기 🚿

- **목표**: 제한된 샤워 시간 내에 노래를 **끝까지(100%) 재생** (노래 자체는 9초로 짧게)
- **핵심 조작 (홀드 방식)**: 노래는 **기본 재생 상태**. 화면의 [⏸ 일시정지] 버튼을 **누르고 있는 동안만 멈추고, 손을 떼면 즉시 다시 재생**됨. 즉 플레이어는 "숨기는 순간"에만 능동적으로 개입 — 몰래춤추기의 반전 구조
- **화면**: 샤워장 배경, 플레이어 캐릭터, 상단에 노래 진행 바(0~100%). 오른쪽 정문과 옆 칸 커튼, 두 지점 중 하나에서 선배가 나타난다
- **선배 등장**: 예고 없이 정문 또는 옆 칸 커튼에 갑자기 등장. 등장 순간부터 반응 유예(reactMs) 안에 일시정지 버튼을 누르면 세이프 (커튼 쪽은 더 가까운 만큼 유예가 더 짧다)
  - 유예 시간 안에 못 누르면 발각, 게임 오버
  - 선배가 퇴장하기 전에 손을 떼도 발각 (선배가 화면에 있는 동안 계속 홀드 유지해야 함)
- **노래**: 「차는 두고 가」 음원 사용 (사용자가 음원 파일과 권리 처리를 직접 제공 — `src/assets/audio/song.mp3` 경로에 배치, 개발 중엔 임시 플레이스홀더 루프로 대체 가능하게 오디오 키 추상화)
- **난이도**: 일차↑ → 선배 등장 횟수↑, 반응 유예↓, 체류 시간↑ (홀드 중엔 노래가 안 차므로 체류가 길수록 시간 압박)

### Q2. 복도에서 경례 대신 인사로 받기 🫡

- **목표**: 복도에서 마주치는 **후배(1학년)의 경례를 "인사(고개 까딱)"로 받는 것**을 N회 성공 (N은 일차에 비례, 2~4회). 단, 그 순간을 선배에게 들키면 안 됨
- **등장 인물**: 후배(경례해 옴), 선배(수시로 복도에 나타남)
- **화면**: 복도 배경, 전방에서 후배가 접근해 경례 모션. 좌/우 양쪽 문 중 무작위 지점에서 선배가 예고 없이 등장/퇴장을 반복 (선배 존재 여부가 이 퀘스트의 리스크 판단 기준)
- **조작**: 하단 버튼 2개 — [🙇 인사로 받기] / [🫡 경례로 받기]
  - 버튼은 **후배가 경례한 뒤에만** 유효. **후배가 경례하기 전에 먼저 경례 버튼을 누르면** = 후배한테 선경례한 꼴 → 놀림받고 **HP -10**
- **판정 매트릭스**:
  | 상황 | 결과 |
  |---|---|
  | 후배 경례 → 인사로 받기, **선배 없음** | ✅ 성공 카운트 +1 (퀘스트 목표) |
  | 후배 경례 → 인사로 받기, **선배가 보고 있음** | ☠️ **게임 오버** |
  | 후배 경례 → 경례로 받기 | 세이프, 카운트 없음 (시간만 소모) |
  | 후배 경례 전에 경례 버튼 선입력 | 😅 HP -10 (선경례 굴욕) |
  | 후배 경례를 무시(타임아웃) | HP -5 (건방지다고 소문남) |
- **긴장 설계**: 선배는 사전 경고 없이 좌/우 어느 문에서든 갑자기 나타날 수 있음. 인사 모션 중에 선배가 갑자기 나타날 수도 있음 → "지금 인사해도 되나?"를 계속 도박하게 만듦
- **난이도**: 일차↑ → 선배 등장 빈도↑, 후배 접근 간격↓, 필요 성공 횟수↑

### Q3. 몰래 결식하고 전자레인지 돌리기 🍜

- **목표**: 전자레인지 조리 게이지 100% 채우기 (조리 시간 6~8.5초로 짧게)
- **화면**: 좌측 세탁실(은신처), 우측 전자레인지. 플레이어는 두 위치를 오감. 복도의 좌/중앙/우 세 지점 중 하나에서 선배가 나타난다
- **조작**: 화면 좌/우 터치로 위치 전환. 전자레인지 앞에 있을 때만 게이지가 참
- **선배 등장**: 예고 없이 복도의 무작위 지점에 갑자기 등장. 등장 순간부터 반응 유예(reactMs) 안에 세탁실로 피하면 세이프
  - 유예 시간 안에 세탁실로 피하지 못하면 발각 = 게임 오버
  - 함정 요소: 게이지 100% 도달 시 "삐-" 소리가 남 → 삐 소리가 울리는 동안은 세탁실에 숨어 있어도 선배가 있으면 발각 (반응 유예 없이 즉시). 95%쯤에서 선배가 오면 극한의 긴장 연출
- **난이도**: 일차↑ → 선배 방문 빈도↑, 반응 유예↓, 체류 시간↑

### Q4. 태권도장(무도관)까지 걸어가기 🥋

- **목표**: 도착 게이지(거리) 100% 채우기. **걸어서** 채우는 것이 원칙 (총 이동 시간 11~15초로 대폭 단축 — 짧고 빠르게)
- **핵심 룰**:
  - 평소엔 [🚶 걷기] 상태 유지 → 게이지가 천천히 참
  - **선배가 나타나면 즉시 [🏃 뛰기]로 전환** (이동 간 구보 원칙 — 걷다 들키면 게임 오버)
  - 뛰는 동안 **HP 지속 감소** (초당 -3). HP 0 = 게임 오버 (HP는 그날 리셋된 100에서 시작)
  - 선배가 사라지면 다시 걷기로 HP 소모 정지
- **조작**: 화면 홀드 = 뛰기, 떼면 걷기 (홀드 방식, 몰래춤추기 감성)
- **선배 등장**: 예고 없이 좌/우/정면 중 무작위 방향에서 갑자기 등장. 등장 순간부터 반응 유예(graceMs, 0.26~0.48초) 안에 뛰기로 전환하지 못하면 발각 — 순수 반응속도 승부
- **난이도**: 일차↑ → 선배 등장 빈도/체류 시간↑, 반응 유예↓

### Q5. 옆방(1학년 방) 벽 치기 💥 (10일차부터 등장)

- **상황**: 소등 후 침대에 누워서, **옆방과 맞닿은 벽을 주먹으로 쿵쿵 치는 장난**. 옆방은 1학년 방이라고 알고 있지만… **오늘 그 방에 선배가 놀러와 있을 수도 있다.**
- **목표**: 정해진 횟수(N회, 4~6회)만큼 벽을 친다. N회 모두 무사하면 클리어
- **핵심 메커닉 — 순수 운빨 게임**:
  - [👊 벽 치기] 버튼을 누를 때마다 판정 롤
  - **1학년만 있음** (확률 대부분) → 벽 너머에서 "충…충성!" 리액션, 카운트 +1
  - **선배가 있음** (일차에 비례해 확률↑) → 벽 너머에서 정적… 문 벌컥 → ☠️ **즉사, 게임 오버**
  - 스킬 개입 없음. 순도 100% 도박. 대신 **연출로 긴장 극대화**: 벽 치기 → 0.5~1.5초 랜덤 딜레이의 정적 → 결과 공개. 정적 동안 심장박동 SFX
  - 선택 요소(연출): 중간에 "그만두기" 버튼 노출 — 누르면 퀘스트 실패는 아니지만 그날 클리어 보상 없이… 은 없음. **그만두기 불가, 끝까지 가야 함**이 이 퀘스트의 아이덴티티 (일단 시작하면 되돌릴 수 없다)
- **확률 설계** (config 상수): 1회당 선배 확률 `p = min(0.25, 0.05 + (day - 10) * 0.01)` — 5연속 생존 확률이 일차에 따라 자연스럽게 감소하도록 튜닝
- **난이도**: 일차↑ → 선배 확률↑, 요구 타수↑

---

## 5. 미니 퀘스트 상세 명세

공통: 화면 상단에서 알림이 "띠링" 하고 내려오는 연출 → 메인 씬 pause → 미니 씬 오버레이.

### M1. 카톡 답장하기 💬 (타자)

- **목표**: **11초** 안에 제시된 문장을 정확히 타이핑 (쫄리지만 타이핑할 여유는 있게 — 시간이 이 게임의 전부)
- **화면**: 실제 카카오톡 채팅 캡처 기반 UI (사용자가 캡처 이미지 제공 — `src/assets/img/kakao/` 배치). 선배의 메시지가 오고, 답장할 문장이 제시됨
- **연출**: 남은 시간을 큰 숫자 + 째깍 SFX + 3초 이하부터 화면 붉은 펄스로 압박
- **구현**: 숨겨진 `<input>` DOM 요소에 포커스 → 모바일 가상 키보드, input 이벤트로 대조. 오타는 빨간 표시, 백스페이스 허용. **씬 진입 즉시 키보드가 올라오도록** 처리 (키보드 뜨는 시간도 11초에 포함되면 억울하므로, 키보드 focus 확인 후 타이머 시작)
- **문장 풀**: 짧은 한국어 문장 15개 내외 (예: "네 알겠습니다", "지금 바로 가겠습니다", "죄송합니다 확인했습니다") — config에 배열로
- **실패**: 시간 초과 또는 오타 상태로 전송 → 실패
- **난이도**: 일차↑ → 문장 길이↑ (시간은 11초 고정 유지)

### M2. 투표하기 🗳️ (함정 선지 독해)

- **목표**: 제한 시간 11.5초(일차↑에 따라 최소 7초까지 감소) 안에 **의미상 올바른 선지** 선택
- **핵심**: 선지가 다중 부정문으로 꼬여 있음. 예시:
  - 질문: "특강 만족도 조사 — 당신은 졸지 않았다. 올바른 선지는?"
  - 선지 A: "특강 시간에 졸지 않은 것 같지 않다" (= 졸았다)
  - 선지 B: "특강 시간에 졸지 않았다고 하기 어렵지 않다" (= 안 졸았다)
- **문제 풀**: 10문제 내외를 config에 정의 (질문 / 선지 2~3개 / 정답 인덱스)
- **난이도**: 일차↑ → 부정 중첩 횟수↑, 시간↓

### M3. 단체 채팅방 사진 고르기 📸

- **컨셉**: 선배가 단체 채팅방에 화를 내며 사진을 올림 → 정답 사진을 4.5초 안에 골라야 함
- **연출 포맷** (카톡 채팅 형식):
  - 채팅방 제목: **"너네 장난하냐?"**
  - 선배 메시지: **"내가 이렇게 방 정리하라고 시켰냐?"** + 사진들
  - 지시문: "제대로 정리된 관물대를 고르세요"
- **모드 2종** (문제마다 랜덤):
  1. **고르기 모드**: 사진 4장 중 올바른 1장 터치 (4.5초)
  2. **틀린그림찾기 모드**: 사진 1장에서 **잘못된 부분을 직접 터치** (예: 관물대 사진에서 삐뚤어진 모포, 안 잠긴 서랍 — 히트 영역을 이미지 위 투명 존으로 정의) (7초)
- **문제 소재**: 관물대 정리, 경례 자세, 정복 다림질, 침구 각 등 — 일러스트/합성으로 제작하고 정답·오답 포인트는 명확한 시각 차이로
- **난이도**: 일차↑ → 차이가 미묘해짐, 고르기 모드 선택지 6장으로 증가

---

## 6. 에셋 가이드

### 사용자 제공 에셋 (개발자가 만들지 않음 — 자리만 잡아둘 것)
| 에셋 | 경로 | 비고 |
|---|---|---|
| 「차는 두고 가」 음원 | `src/assets/audio/song.mp3` | 사용자가 권리 처리 후 파일 제공. **제공 전까지는 자체 칩튠 루프 플레이스홀더** 사용, 오디오 키를 추상화해 파일만 교체하면 되게 |
| 카카오톡 UI 캡처 | `src/assets/img/kakao/` | M1, M3 배경. 제공 전까지는 단색 메신저풍 목업 |

- 개인 프로젝트이므로 저작권 처리는 사용자 책임 범위로 두고, 코드 구조상 **에셋 교체가 쉬운 구조**(키 기반 로딩, 경로 상수화)만 보장할 것

### 자체 제작 에셋
- 초기 버전은 Phaser Graphics/도형 + 이모지 텍스트 + 단색 그라디언트 배경으로 **플레이 가능한 프로토타입** 우선 완성, 이후 교체 가능하게 텍스처 키 추상화
- 미연시 레이아웃: 하단 30% 대사창(반투명 검정 + 이름표), 중앙 캐릭터 스탠딩, 배경 일러스트
- 캐릭터: 플레이어(2학년), 후배(1학년), 선배(3~4학년, 위압감) — 실루엣/색상으로 즉시 구분되게

### 사운드
- Web Audio 기반. SFX는 자체 제작 (jsfxr류 생성 권장)
- 필수 SFX: 문 여는 소리(선배 갑작스런 등장), 전자레인지 삐, 카톡 알림음, 벽치기 쿵, 심장박동(Q5 정적 연출), 성공 팡파레, 게임오버 스팅어
- 음소거 토글 버튼 상시 표시

---

## 7. 온라인 리더보드 (Cloudflare Pages Functions + D1)

호스팅과 같은 Cloudflare 프로젝트 안에서 해결한다. 외부 BaaS 불필요.

### D1 스키마 (`migrations/0001_leaderboard.sql`)

```sql
CREATE TABLE leaderboard (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  nickname TEXT NOT NULL CHECK (length(nickname) BETWEEN 1 AND 12),
  days INTEGER NOT NULL CHECK (days BETWEEN 1 AND 999),
  play_ms INTEGER NOT NULL CHECK (play_ms > 0),
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_rank ON leaderboard (days DESC, play_ms ASC);
```

### API (`functions/api/leaderboard.ts`)

- **GET** `/api/leaderboard` → 상위 50개, `ORDER BY days DESC, play_ms ASC` (JSON)
- **POST** `/api/leaderboard` → `{ nickname, days, play_ms }` 삽입
  - 서버측 검증: 닉네임 길이/금칙어 최소 필터, days 범위, `play_ms >= days * 최소플레이시간(예: 15초)` — 말도 안 되는 기록 차단
  - 간단 레이트리밋: IP당 1분 1회 (KV 없이도 D1에 최근 insert 조회로 처리 가능한 수준이면 그렇게, 과하면 생략)
- 클라이언트(`Leaderboard.ts`)는 같은 오리진 `/api/...` fetch — CORS 설정 불필요
- **오프라인/실패 대응**: 등록 실패 시 localStorage 큐잉 후 다음 접속 시 재시도. 조회 실패 시 로컬 최고기록만 표시
- 완전한 안티치트는 범위 외 (클라이언트 게임의 한계로 명시)

### 로컬 개발
- `wrangler pages dev -- npm run dev` 또는 빌드 후 `wrangler pages dev dist` 로 Functions + D1(로컬 미니플레어) 동시 테스트
- `wrangler.toml`에 D1 바인딩 (`binding = "DB"`)

---

## 8. PWA 요구사항

- `vite-plugin-pwa`, `registerType: 'autoUpdate'`
- manifest: `display: standalone`, `orientation: portrait`, 테마 컬러, 아이콘 192/512
- 게임 에셋 precache → **설치 후 오프라인 플레이 가능** (리더보드만 온라인 필요)
- `/api/*` 경로는 **Service Worker 캐시에서 제외** (NetworkOnly) — 리더보드 stale 방지
- iOS Safari 대응: `apple-touch-icon`, `apple-mobile-web-app-capable` 메타 태그

---

## 9. 배포 (Cloudflare Pages)

1. GitHub 저장소를 Cloudflare Pages에 연결 (빌드 명령 `npm run build`, 출력 `dist`, `functions/` 자동 인식)
2. `main` push → 자동 배포. PR 브랜치는 프리뷰 URL 자동 생성 (폰 테스트에 유용)
3. D1 데이터베이스 생성 후 Pages 프로젝트에 바인딩 (`DB`)
4. 커스텀 도메인: Pages 프로젝트에 `ygt2025.rokafa.app` 추가 → rokafa.app이 Cloudflare에 있으면 DNS 레코드 자동 생성
5. 프로덕션 마이그레이션: `wrangler d1 migrations apply <db-name> --remote`

---

## 10. 구현 순서 (Claude Code 작업 가이드)

1. **스캐폴딩**: Vite + TS + Phaser + PWA 플러그인, Boot/Title 씬, wrangler.toml, Cloudflare Pages 배포 연결 문서화 → 폰에서 접속 확인 가능한 상태 먼저
2. **코어 시스템**: GameState(일수별 HP 리셋 포함), QuestManager, DayIntro/Result 씬, DialogueBox UI
3. **메인 퀘스트 1개 완성 (Q4 태권도 추천 — 메커닉 최단순)** → 하루 루프 전체가 도는 수직 슬라이스 확보
4. **나머지 메인 퀘스트** Q1 → Q3 → Q2 → Q5 순서
5. **미니 퀘스트 3종** + 인터럽트 시스템
6. **리더보드** (D1 마이그레이션 + Pages Function + 클라이언트)
7. **사운드/연출 폴리싱**, 난이도 밸런스 (config.ts 상수 튜닝)

### 코딩 규칙
- 밸런스 수치(확률, 시간, HP 감소량 등)는 **전부 `config.ts`에 상수/함수로** — 씬 코드에 매직넘버 금지
- 씬 간 통신은 GameState 싱글턴 + Phaser 이벤트만 사용
- 모바일 우선: 터치 타깃 최소 88px, 폰트 최소 28px(720px 기준)
- 사용자 제공 에셋(음원, 카톡 캡처)은 키 기반 로딩으로 추상화 — 파일 교체만으로 적용되게
- 각 단계마다 `npm run build` 통과 (strict TypeScript)

---

## 11. 범위 외 (하지 않는 것)

- 계정 시스템 / 소셜 로그인
- 네이티브 앱 빌드 (Capacitor 등)
- 서버 사이드 완전 안티치트
- 가로 모드 대응
