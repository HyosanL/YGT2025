{
  "name": "ygt2025",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && tsc -p functions --noEmit && vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev dist",
    "d1:migrate:local": "wrangler d1 migrations apply ygt2025-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply ygt2025-db --remote"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0",
    "wrangler": "^4.0.0"
  }
}
